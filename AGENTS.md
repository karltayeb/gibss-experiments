# Repo Instructions

- Always use `uv` for Python execution, testing, and environment-managed commands in this repository.
- Each subdirectory is it's own analysis. You should treat the subdirectory as root e.g. run snakemake pipeline from ./twogroup_experiments/ not from .
