# 000_markov SER Calibration Batch — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the `000_markov` L=1 SER simulation batch — synthetic Markov designs × correlation × signal (expected-logBF) × intercept — with exact-LRT effect sizing frozen into the config.

**Architecture:** A new `logbf_sizing` module computes the effect size `b` that hits a target expected logBF (= expected LRT `Λ=n·MI`, monotone). Its frozen values are baked into per-(design, b0) enrichment families in `library.yaml` (reusing the existing `uniform_single_effect` sampler). `viz_dims` gains `rho`, binary-frequency design splitting, a derived `logbf` facet dimension (recomputed via `logbf_sizing`, single-source), and a `null_score` method alias. A new `experiments/000_markov.yaml` wires designs × enrichments × 6 methods × plots. The dead `000_global_local` experiment is removed.

**Tech Stack:** Python, numpy, scipy (`brentq`), polars, pyyaml, pytest, marimo (existing). `uv run` for all commands.

## Global Constraints

- Use `uv run` for every python/pytest invocation (never bare `python`/`pytest`).
- n=1000, p=500 for all 000_markov designs.
- Effect sizing = exact LRT `Λ(b)=n·MI(x;y)`; **glmbf must NOT be imported or added as a dependency**.
- Methods: fixed prior `estimate_prior_variance=false`, `prior_variance=1.0`; `center=false` for all logistic methods except `null_score` (`center=true`).
- The 6 methods: `linear`, `null_score` (= irls n_outer=1, center=true), `irls` (n_outer=50, center=false), `localjj`, `globaljj`, `quadrature`.
- Grid: design ∈ {gaussian, uniform, binary q=0.5, binary q=0.1}; rho ∈ {0, 0.5, 0.8, 0.9, 0.95, 0.99}; target logBF ∈ {4, 8, 16, 32, 64}; b0 ∈ {0, −2, −4}; + null per (design, b0).
- Replicates: `replicates_per_batch=50`, `n_batches=2`.
- Frozen effect-size table (n=1000), the authoritative values to bake:

| design | b0 | L4 | L8 | L16 | L32 | L64 |
|---|---:|---:|---:|---:|---:|---:|
| gaussian | 0 | 0.180 | 0.256 | 0.366 | 0.531 | 0.789 |
| uniform | 0 | 0.311 | 0.441 | 0.628 | 0.902 | 1.316 |
| binary q=0.5 | 0 | 0.179 | 0.254 | 0.361 | 0.514 | 0.740 |
| binary q=0.1 | 0 | 0.299 | 0.425 | 0.607 | 0.874 | 1.286 |
| gaussian | −2 | 0.275 | 0.387 | 0.545 | 0.766 | 1.091 |
| uniform | −2 | 0.477 | 0.675 | 0.953 | 1.347 | 1.910 |
| binary q=0.5 | −2 | 0.277 | 0.392 | 0.556 | 0.793 | 1.137 |
| binary q=0.1 | −2 | 0.440 | 0.613 | 0.851 | 1.180 | 1.649 |
| gaussian | −4 | 0.621 | 0.830 | 1.086 | 1.405 | 1.829 |
| uniform | −4 | 1.124 | 1.543 | 2.077 | 2.735 | 3.537 |
| binary q=0.5 | −4 | 0.674 | 0.951 | 1.337 | 1.853 | 2.502 |
| binary q=0.1 | −4 | 0.974 | 1.313 | 1.752 | 2.316 | 3.039 |

---

## File Structure

- `simulations/effect/logbf_sizing.py` (new) — exact-LRT solver, `nearest_logbf`, frozen-table generator. One responsibility: map between effect size `b` and expected logBF.
- `core.py` (modify) — re-export the design key helper if needed (no change expected; designs already exported).
- `viz_dims.py` (modify) — add `rho`, binary-freq design split, `logbf` dim, `null_score` alias.
- `experiments/library.yaml` (modify) — add 24 designs + 12 signal enrichment families + 4 null families.
- `experiments/000_markov.yaml` (new) — the supercollection (collections, methods, plots).
- `experiments/000_global_local.yaml` (delete).
- `tests/test_logbf_sizing.py` (new), `tests/test_viz_dims.py` (modify), `tests/test_000_markov_config.py` (new).

---

## Task 1: `logbf_sizing` module

**Files:**
- Create: `simulations/effect/logbf_sizing.py`
- Test: `tests/test_logbf_sizing.py`

**Interfaces:**
- Produces:
  - `MARGINALS: dict[str, tuple]` — design key → `(atoms, probs)`.
  - `expected_lrt(design: str, b0: float, b: float, n: int) -> float`
  - `solve_b(design: str, target_logbf: float, b0: float, n: int) -> float`
  - `nearest_logbf(design: str, b0: float, b: float, n: int, grid=(4,8,16,32,64)) -> int` — the grid logBF whose `Λ` is closest to `Λ(b)`; returns `0` if `b==0`.
  - design keys: `"gaussian"`, `"uniform"`, `"binary q=0.5"`, `"binary q=0.1"`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_logbf_sizing.py
import numpy as np
import pytest
from simulations.effect import logbf_sizing as S

DESIGNS = ["gaussian", "uniform", "binary q=0.5", "binary q=0.1"]


@pytest.mark.parametrize("design", DESIGNS)
@pytest.mark.parametrize("b0", [0.0, -2.0, -4.0])
@pytest.mark.parametrize("target", [4, 8, 16, 32, 64])
def test_solve_b_roundtrips(design, b0, target):
    b = S.solve_b(design, target, b0, 1000)
    assert S.expected_lrt(design, b0, b, 1000) == pytest.approx(target, abs=1e-3)


def test_expected_lrt_zero_at_null_and_monotone():
    assert S.expected_lrt("gaussian", -2.0, 0.0, 1000) == pytest.approx(0.0, abs=1e-9)
    vals = [S.expected_lrt("gaussian", -2.0, b, 1000) for b in [0.1, 0.5, 1.0, 2.0, 4.0]]
    assert all(a < b for a, b in zip(vals, vals[1:]))


def test_nearest_logbf_recovers_grid():
    # the frozen b for L16 must map back to 16
    b = S.solve_b("binary q=0.1", 16, -2.0, 1000)
    assert S.nearest_logbf("binary q=0.1", -2.0, b, 1000) == 16
    assert S.nearest_logbf("gaussian", 0.0, 0.0, 1000) == 0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_logbf_sizing.py -q`
Expected: FAIL with `ModuleNotFoundError: No module named 'simulations.effect.logbf_sizing'`.

- [ ] **Step 3: Write the module**

```python
# simulations/effect/logbf_sizing.py
"""Map effect size <-> expected univariate logBF via the exact expected LRT
(Λ = n·MI), monotone in b. glmbf-free (numpy + scipy only).

Λ(b) = n·[H(p̄) − E_x H(p_x)],  p_x = σ(b0 + b·x),  expectations over the causal
feature's marginal. Monotone in b, saturating at the deviance n·H(p̄_∞), so every
grid rung is reachable by a 1-D root find — no Wald ceiling (see
notes/2026-06-27-univariate-logbf-z-grid.md).
"""
from __future__ import annotations

import numpy as np
from scipy.optimize import brentq

_GRID = (4, 8, 16, 32, 64)


def _gaussian():
    x = np.linspace(-8.0, 8.0, 1200)
    w = np.exp(-x * x / 2.0)
    return x, w / w.sum()


def _uniform():
    x = np.linspace(-1.0, 1.0, 1200)
    return x, np.full(1200, 1.0 / 1200)


def _binary(q):
    return np.array([-1.0, 1.0]), np.array([1.0 - q, q])


MARGINALS = {
    "gaussian": _gaussian(),
    "uniform": _uniform(),
    "binary q=0.5": _binary(0.5),
    "binary q=0.1": _binary(0.1),
}


def _sigmoid(z):
    return np.where(z >= 0, 1.0 / (1.0 + np.exp(-z)), np.exp(z) / (1.0 + np.exp(z)))


def _H(p):
    p = np.clip(p, 1e-15, 1.0 - 1e-15)
    return -(p * np.log(p) + (1.0 - p) * np.log(1.0 - p))


def expected_lrt(design: str, b0: float, b: float, n: int) -> float:
    """Λ = n·MI(x;y). Monotone in |b|."""
    x, pw = MARGINALS[design]
    px = _sigmoid(b0 + b * x)
    pbar = float(np.sum(pw * px))
    return float(n) * (_H(pbar) - float(np.sum(pw * _H(px))))


def solve_b(design: str, target_logbf: float, b0: float, n: int) -> float:
    """Effect size with Λ(b) = target_logbf (positive-b branch)."""
    if expected_lrt(design, b0, 60.0, n) < target_logbf:
        raise ValueError(
            f"target logBF {target_logbf} exceeds deviance ceiling for "
            f"{design!r}, b0={b0}, n={n}"
        )
    return float(brentq(lambda bb: expected_lrt(design, b0, bb, n) - target_logbf, 1e-6, 60.0))


def nearest_logbf(design: str, b0: float, b: float, n: int, grid=_GRID) -> int:
    """The grid rung whose Λ is closest to Λ(b). 0 for the null (b==0)."""
    if b == 0.0:
        return 0
    lam = expected_lrt(design, b0, abs(b), n)
    return int(min(grid, key=lambda g: abs(g - lam)))


def frozen_table(n: int = 1000, b0s=(0.0, -2.0, -4.0), grid=_GRID) -> list[dict]:
    """Generator for the values baked into library.yaml."""
    rows = []
    for design in MARGINALS:
        for b0 in b0s:
            rec = {"design": design, "b0": b0}
            for g in grid:
                rec[f"L{g}"] = round(solve_b(design, g, b0, n), 3)
            rows.append(rec)
    return rows


if __name__ == "__main__":
    for r in frozen_table():
        print(r)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_logbf_sizing.py -q`
Expected: PASS (all parametrizations).

- [ ] **Step 5: Verify the generator reproduces the frozen table**

Run: `uv run python -m simulations.effect.logbf_sizing`
Expected: prints rows whose `L4..L64` match the Global Constraints table (e.g. `gaussian b0=-2 → L16 0.545`).

- [ ] **Step 6: Commit**

```bash
git add simulations/effect/logbf_sizing.py tests/test_logbf_sizing.py
git commit -m "feat(sizing): logbf_sizing module (exact-LRT effect sizing, glmbf-free)"
```

---

## Task 2: `viz_dims` — rho, binary-freq, logbf, null_score

**Files:**
- Modify: `viz_dims.py`
- Test: `tests/test_viz_dims.py`

**Interfaces:**
- Consumes: `simulations.effect.logbf_sizing.nearest_logbf` (Task 1).
- Produces: `sim_dims(coord)` now includes `rho` (float), refined `design`
  (`"binary_q0.5"`, `"binary_q0.1"` for binary; unchanged otherwise), and
  `logbf` (int, from `nearest_logbf`). `method_dims(coord)` returns
  `family="null_score"` when the method is `run_irls_method` with `n_outer==1`.

- [ ] **Step 1: Write the failing test (append to tests/test_viz_dims.py)**

```python
def test_sim_dims_rho_binary_logbf():
    from viz_dims import sim_dims
    coord = {
        "design": {"function": "binary_markov_X",
                   "arguments": {"n": 1000, "p": 500, "rho": 0.9, "freq": 0.1}},
        "enrichment": {"arguments": {"causal_effect": 1.180}, "intercept": -2.0},
    }
    d = sim_dims(coord)
    assert d["design"] == "binary_q0.1"
    assert d["rho"] == 0.9
    assert d["logbf"] == 32          # frozen b for binary q=0.1, b0=-2, L32
    assert d["signal"] is True


def test_sim_dims_null_logbf_zero():
    from viz_dims import sim_dims
    coord = {
        "design": {"function": "gaussian_markov_X",
                   "arguments": {"n": 1000, "p": 500, "rho": 0.0}},
        "enrichment": {"arguments": {"causal_effect": 0.0}, "intercept": 0.0},
    }
    assert sim_dims(coord)["logbf"] == 0


def test_method_dims_null_score_alias():
    from viz_dims import method_dims
    coord = {"function": "run_irls_method",
             "kwargs": {"ser_cadence": "block", "n_outer": 1, "L": 1, "center": True}}
    assert method_dims(coord)["family"] == "null_score"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_viz_dims.py -q`
Expected: FAIL (`design` is `"binary"`, no `rho`/`logbf` keys, family `"irls"`).

- [ ] **Step 3: Edit `viz_dims.py`**

In `method_dims`, after computing `family` and before building `semantic`, add the alias:

```python
    if family == "irls" and k.get("n_outer") == 1:
        family = "null_score"
```

In `sim_dims`, replace the body with (adds rho, binary-freq design, logbf):

```python
def sim_dims(coord: dict) -> dict:
    from simulations.effect import logbf_sizing
    d = coord["design"]
    e = coord["enrichment"]
    dargs = d.get("arguments") or {}
    b = float(e["arguments"].get("causal_effect", 0.0))
    b0 = float(e["intercept"])
    design = _DESIGN_NAMES.get(d["function"], d["function"])
    if d["function"] == "binary_markov_X":
        design = f"binary_q{dargs.get('freq', 0.5):g}"
    n = int(dargs.get("n", 0)) or None
    sizing_key = {"binary_q0.5": "binary q=0.5", "binary_q0.1": "binary q=0.1"}.get(
        design, design)
    logbf = (logbf_sizing.nearest_logbf(sizing_key, b0, b, n)
             if (n and sizing_key in logbf_sizing.MARGINALS) else 0)
    semantic = {
        "design": design,
        "intercept": b0,
        "b": b,
        "signal": b != 0.0,
        "rho": float(dargs.get("rho", 0.0)),
        "logbf": int(logbf),
    }
    raw = {f"d_{key}": v for key, v in dargs.items()}
    raw |= {f"e_{key}": v for key, v in (e.get("arguments") or {}).items()}
    return {**semantic, **raw}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_viz_dims.py -q`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add viz_dims.py tests/test_viz_dims.py
git commit -m "feat(viz_dims): rho, binary-freq design split, logbf dim, null_score alias"
```

---

## Task 3: `library.yaml` — designs + enrichment families

**Files:**
- Modify: `experiments/library.yaml`
- Test: `tests/test_000_markov_config.py` (created here, extended in Task 4)

**Interfaces:**
- Consumes: design samplers `gaussian_markov_X`, `uniform_markov_X`, `binary_markov_X` (already exported from `core`); `uniform_single_effect`.
- Produces: 24 design keys `{gaussian,uniform,binary_q0.5,binary_q0.1}_markov_n1000_p500_rho{0,0.5,0.8,0.9,0.95,0.99}`; enrichment groups `ser_{design}_b0{0,m2,m4}` and `ser_{design}_null` for each of the 4 design types.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_000_markov_config.py
from experiments import loader


def test_markov_designs_and_enrichments_present():
    lib = loader.load_library()
    assert "gaussian_markov_n1000_p500_rho0.9" in lib["designs"]
    assert "binary_q0.1_markov_n1000_p500_rho0.99" in lib["designs"]
    groups = lib["enrichment_groups"]
    # 4 designs x 3 b0 signal groups + 4 null groups
    assert "ser_gaussian_b0_0" in groups
    assert "ser_binary_q0.1_b0_m4" in groups
    assert "ser_uniform_null" in groups
    # signal group has 5 logBF rungs; null group has 3 intercepts
    assert len(groups["ser_gaussian_b0_0"]) == 5
    assert len(groups["ser_gaussian_null"]) == 3


def test_frozen_b_values_match_sizing():
    from simulations.effect import logbf_sizing as Sz
    lib = loader.load_library()
    # one spot check: gaussian b0=-2 L16 enrichment must carry b≈0.545
    name = "ser__gaussian__b0=-2__b=0.545"
    assert name in lib["enrichments"]
    assert lib["enrichments"][name]["arguments"]["causal_effect"] == 0.545
    assert Sz.nearest_logbf("gaussian", -2.0, 0.545, 1000) == 16
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_000_markov_config.py -q`
Expected: FAIL (designs/enrichments absent).

- [ ] **Step 3: Append designs to `experiments/library.yaml`** (under `designs:`)

Add a banner comment and these 24 entries (rho ∈ {0, 0.5, 0.8, 0.9, 0.95, 0.99}). Use this exact pattern for every (type, rho) — shown for all four types at rho=0.9; **repeat for each rho value**, substituting `rho` and the `rho<v>` in the key:

```yaml
  # --- 000_markov designs: n=1000 p=500, rho sweep (generated, see logbf_sizing) ---
  gaussian_markov_n1000_p500_rho0:    { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0 } }
  gaussian_markov_n1000_p500_rho0.5:  { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0.5 } }
  gaussian_markov_n1000_p500_rho0.8:  { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0.8 } }
  gaussian_markov_n1000_p500_rho0.9:  { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0.9 } }
  gaussian_markov_n1000_p500_rho0.95: { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0.95 } }
  gaussian_markov_n1000_p500_rho0.99: { function: gaussian_markov_X, arguments: { n: 1000, p: 500, rho: 0.99 } }
  uniform_markov_n1000_p500_rho0:     { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0 } }
  uniform_markov_n1000_p500_rho0.5:   { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0.5 } }
  uniform_markov_n1000_p500_rho0.8:   { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0.8 } }
  uniform_markov_n1000_p500_rho0.9:   { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0.9 } }
  uniform_markov_n1000_p500_rho0.95:  { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0.95 } }
  uniform_markov_n1000_p500_rho0.99:  { function: uniform_markov_X, arguments: { n: 1000, p: 500, rho: 0.99 } }
  binary_q0.5_markov_n1000_p500_rho0:    { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0,    freq: 0.5 } }
  binary_q0.5_markov_n1000_p500_rho0.5:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.5,  freq: 0.5 } }
  binary_q0.5_markov_n1000_p500_rho0.8:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.8,  freq: 0.5 } }
  binary_q0.5_markov_n1000_p500_rho0.9:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.9,  freq: 0.5 } }
  binary_q0.5_markov_n1000_p500_rho0.95: { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.95, freq: 0.5 } }
  binary_q0.5_markov_n1000_p500_rho0.99: { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.99, freq: 0.5 } }
  binary_q0.1_markov_n1000_p500_rho0:    { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0,    freq: 0.1 } }
  binary_q0.1_markov_n1000_p500_rho0.5:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.5,  freq: 0.1 } }
  binary_q0.1_markov_n1000_p500_rho0.8:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.8,  freq: 0.1 } }
  binary_q0.1_markov_n1000_p500_rho0.9:  { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.9,  freq: 0.1 } }
  binary_q0.1_markov_n1000_p500_rho0.95: { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.95, freq: 0.1 } }
  binary_q0.1_markov_n1000_p500_rho0.99: { function: binary_markov_X, arguments: { n: 1000, p: 500, rho: 0.99, freq: 0.1 } }
```

- [ ] **Step 4: Append enrichment families to `experiments/library.yaml`** (under `enrichments:`)

12 signal families (one per design×b0; `causal_effect` is the frozen 5-value row from the Global Constraints table) + 4 null families:

```yaml
  # --- 000_markov enrichments: exact-LRT sized (n=1000), frozen; see logbf_sizing ---
  ser_gaussian_b0_0:   { function: uniform_single_effect, arguments: { causal_effect: [0.180, 0.256, 0.366, 0.531, 0.789] }, intercept: 0,  name: "ser__gaussian__b0=0__b={causal_effect:g}" }
  ser_gaussian_b0_m2:  { function: uniform_single_effect, arguments: { causal_effect: [0.275, 0.387, 0.545, 0.766, 1.091] }, intercept: -2, name: "ser__gaussian__b0=-2__b={causal_effect:g}" }
  ser_gaussian_b0_m4:  { function: uniform_single_effect, arguments: { causal_effect: [0.621, 0.830, 1.086, 1.405, 1.829] }, intercept: -4, name: "ser__gaussian__b0=-4__b={causal_effect:g}" }
  ser_uniform_b0_0:    { function: uniform_single_effect, arguments: { causal_effect: [0.311, 0.441, 0.628, 0.902, 1.316] }, intercept: 0,  name: "ser__uniform__b0=0__b={causal_effect:g}" }
  ser_uniform_b0_m2:   { function: uniform_single_effect, arguments: { causal_effect: [0.477, 0.675, 0.953, 1.347, 1.910] }, intercept: -2, name: "ser__uniform__b0=-2__b={causal_effect:g}" }
  ser_uniform_b0_m4:   { function: uniform_single_effect, arguments: { causal_effect: [1.124, 1.543, 2.077, 2.735, 3.537] }, intercept: -4, name: "ser__uniform__b0=-4__b={causal_effect:g}" }
  ser_binary_q0.5_b0_0:  { function: uniform_single_effect, arguments: { causal_effect: [0.179, 0.254, 0.361, 0.514, 0.740] }, intercept: 0,  name: "ser__binary_q0.5__b0=0__b={causal_effect:g}" }
  ser_binary_q0.5_b0_m2: { function: uniform_single_effect, arguments: { causal_effect: [0.277, 0.392, 0.556, 0.793, 1.137] }, intercept: -2, name: "ser__binary_q0.5__b0=-2__b={causal_effect:g}" }
  ser_binary_q0.5_b0_m4: { function: uniform_single_effect, arguments: { causal_effect: [0.674, 0.951, 1.337, 1.853, 2.502] }, intercept: -4, name: "ser__binary_q0.5__b0=-4__b={causal_effect:g}" }
  ser_binary_q0.1_b0_0:  { function: uniform_single_effect, arguments: { causal_effect: [0.299, 0.425, 0.607, 0.874, 1.286] }, intercept: 0,  name: "ser__binary_q0.1__b0=0__b={causal_effect:g}" }
  ser_binary_q0.1_b0_m2: { function: uniform_single_effect, arguments: { causal_effect: [0.440, 0.613, 0.851, 1.180, 1.649] }, intercept: -2, name: "ser__binary_q0.1__b0=-2__b={causal_effect:g}" }
  ser_binary_q0.1_b0_m4: { function: uniform_single_effect, arguments: { causal_effect: [0.974, 1.313, 1.752, 2.316, 3.039] }, intercept: -4, name: "ser__binary_q0.1__b0=-4__b={causal_effect:g}" }
  ser_gaussian_null:    { function: uniform_single_effect, arguments: { causal_effect: 0.0 }, intercept: [0, -2, -4], name: "ser__gaussian__b0={intercept:g}_null" }
  ser_uniform_null:     { function: uniform_single_effect, arguments: { causal_effect: 0.0 }, intercept: [0, -2, -4], name: "ser__uniform__b0={intercept:g}_null" }
  ser_binary_q0.5_null: { function: uniform_single_effect, arguments: { causal_effect: 0.0 }, intercept: [0, -2, -4], name: "ser__binary_q0.5__b0={intercept:g}_null" }
  ser_binary_q0.1_null: { function: uniform_single_effect, arguments: { causal_effect: 0.0 }, intercept: [0, -2, -4], name: "ser__binary_q0.1__b0={intercept:g}_null" }
```

- [ ] **Step 5: Run test to verify it passes**

Run: `uv run pytest tests/test_000_markov_config.py -q`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add experiments/library.yaml tests/test_000_markov_config.py
git commit -m "feat(library): 000_markov designs + exact-LRT-sized enrichment families"
```

---

## Task 4: `experiments/000_markov.yaml` + remove `000_global_local`

**Files:**
- Create: `experiments/000_markov.yaml`
- Delete: `experiments/000_global_local.yaml`
- Test: `tests/test_000_markov_config.py` (extend)

**Interfaces:**
- Consumes: designs + enrichment groups (Task 3); method runners `run_linear_susie_method`, `run_irls_method`, `run_logistic_method` (existing); the dims from Task 2 for plot faceting.
- Produces: supercollection `000_markov` resolvable by `loader.load_config` and expandable by `loader.expand_collections`.

- [ ] **Step 1: Write the failing test (extend tests/test_000_markov_config.py)**

```python
def test_000_markov_supercollection_expands():
    cfg = loader.load_config()
    assert "000_markov" in cfg["supercollections"]
    sc = cfg["supercollections"]["000_markov"]
    cols = loader.expand_collections(cfg["library"], "000_markov", sc["collections"])
    # 4 design types x 6 rho = 24 collections
    assert len(cols) == 24
    # each collection unions 15 signal (3 b0 x 5 logBF) + 3 null = 18 sims
    assert len(cols[0]["simulations"]) == 18
    # methods: the 6 requested
    methods = loader.resolve_methods_for_sc(cfg["library"], sc)
    assert len(methods) == 6


def test_000_global_local_removed():
    import pathlib
    assert not (pathlib.Path("experiments") / "000_global_local.yaml").exists()
```

(If `expand_collections`/`resolve_methods_for_sc` return shapes differ, adjust the
assertions to the actual structure — the intent is: 24 collections, 6 methods.)

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_000_markov_config.py::test_000_markov_supercollection_expands -q`
Expected: FAIL (`000_markov` absent).

- [ ] **Step 3: Create `experiments/000_markov.yaml`**

```yaml
# 000_markov — L=1 SER calibration batch. Synthetic Markov designs x correlation
# x signal (exact-LRT-sized expected logBF) x intercept. Effects frozen in
# library.yaml (see simulations/effect/logbf_sizing.py and
# notes/2026-06-27-univariate-logbf-z-grid.md). n=1000, p=500.
_anchors:
  default_args: &default_args
    min_log_bf: 2.0
    max_cs_size: 10000
    max_fdp: 0.5
    cs_beta: 0.95

supercollections:
  000_markov:
    n_batches: 2   # 2 x 50 = 100 replicates per simulation
    collections:
      - name: gaussian
        over:
          design: [gaussian_markov_n1000_p500_rho0, gaussian_markov_n1000_p500_rho0.5,
                   gaussian_markov_n1000_p500_rho0.8, gaussian_markov_n1000_p500_rho0.9,
                   gaussian_markov_n1000_p500_rho0.95, gaussian_markov_n1000_p500_rho0.99]
        template:
          enrichment: [ser_gaussian_b0_0, ser_gaussian_b0_m2, ser_gaussian_b0_m4, ser_gaussian_null]
      - name: uniform
        over:
          design: [uniform_markov_n1000_p500_rho0, uniform_markov_n1000_p500_rho0.5,
                   uniform_markov_n1000_p500_rho0.8, uniform_markov_n1000_p500_rho0.9,
                   uniform_markov_n1000_p500_rho0.95, uniform_markov_n1000_p500_rho0.99]
        template:
          enrichment: [ser_uniform_b0_0, ser_uniform_b0_m2, ser_uniform_b0_m4, ser_uniform_null]
      - name: binary_q0.5
        over:
          design: [binary_q0.5_markov_n1000_p500_rho0, binary_q0.5_markov_n1000_p500_rho0.5,
                   binary_q0.5_markov_n1000_p500_rho0.8, binary_q0.5_markov_n1000_p500_rho0.9,
                   binary_q0.5_markov_n1000_p500_rho0.95, binary_q0.5_markov_n1000_p500_rho0.99]
        template:
          enrichment: [ser_binary_q0.5_b0_0, ser_binary_q0.5_b0_m2, ser_binary_q0.5_b0_m4, ser_binary_q0.5_null]
      - name: binary_q0.1
        over:
          design: [binary_q0.1_markov_n1000_p500_rho0, binary_q0.1_markov_n1000_p500_rho0.5,
                   binary_q0.1_markov_n1000_p500_rho0.8, binary_q0.1_markov_n1000_p500_rho0.9,
                   binary_q0.1_markov_n1000_p500_rho0.95, binary_q0.1_markov_n1000_p500_rho0.99]
        template:
          enrichment: [ser_binary_q0.1_b0_0, ser_binary_q0.1_b0_m2, ser_binary_q0.1_b0_m4, ser_binary_q0.1_null]
    methods:
      - linear:
          { function: run_linear_susie_method,
            template: { L: 1, estimate_prior_variance: false, prior_variance: 1.0 } }
      - null_score:
          { function: run_irls_method,
            template: { ser_cadence: block, n_outer: 1, L: 1, center: true,
                        estimate_prior_variance: false, prior_variance: 1.0 } }
      - irls:
          { function: run_irls_method,
            template: { ser_cadence: block, n_outer: 50, L: 1, center: false,
                        estimate_prior_variance: false, prior_variance: 1.0 } }
      - localjj:
          { function: run_logistic_method,
            template: { impl: localjj, L: 1, estimate_prior_variance: false, prior_variance: 1.0 } }
      - globaljj:
          { function: run_logistic_method,
            template: { impl: globaljj, L: 1, estimate_prior_variance: false, prior_variance: 1.0 } }
      - quadrature:
          { function: run_logistic_method,
            template: { impl: logistic_quadrature, L: 1, estimate_prior_variance: false, prior_variance: 1.0 } }
    default_args: *default_args
    # One figure per (design x analysis): facet rho (rows) x logbf (cols), color =
    # family. b0 is filtered to -2 by default; add b0=0 / b0=-4 variants as needed.
    plots:
      gaussian__pip_calibration:
        { analysis: pip_calibration, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2 } }
      gaussian__power_fdp:
        { analysis: power_fdp, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2 } }
      gaussian__cs_power_fdp:
        { analysis: cs_power_fdp, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2 } }
      gaussian__cs_roc:
        { analysis: cs_roc, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2 } }
      gaussian__cs_calibration:
        { analysis: cs_calibration, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2, signal: true } }
      gaussian__cs_coverage_size:
        { analysis: cs_coverage_size, facet_row: rho, facet_col: logbf, color: family,
          filter: { design: gaussian, intercept: -2, signal: true } }
```

(Replicate the six `gaussian__*` plot specs for `uniform`, `binary_q0.5`,
`binary_q0.1` by substituting the design name in both the key and the
`filter.design`. Keep `intercept: -2` as the default b0 view.)

- [ ] **Step 4: Delete the dead experiment**

```bash
grep -rn "000_global_local" experiments/ tests/ *.py   # expect no references
git rm experiments/000_global_local.yaml
```

- [ ] **Step 5: Run config tests**

Run: `uv run pytest tests/test_000_markov_config.py -q`
Expected: PASS (24 collections, 6 methods, file removed).

- [ ] **Step 6: Smoke-test one simulation + fit end-to-end**

Run:
```bash
uv run python -c "
from experiments import loader
import core
cfg = loader.load_config()
sc = cfg['supercollections']['000_markov']
cols = loader.expand_collections(cfg['library'], '000_markov', sc['collections'])
spec = cols[0]['simulations'][0]                 # a core.SimulationSpec
sim = core.simulate(spec, 0)                      # materialize replicate 0
print('collections:', len(cols), 'X shape:', sim.X.shape, 'cases:', int(sim.y.sum()))
"
```
Expected: prints `collections: 24 X shape: (1000, 500) cases: <int>` without error.

- [ ] **Step 7: Commit**

```bash
git add experiments/000_markov.yaml tests/test_000_markov_config.py
git commit -m "feat(000_markov): SER calibration experiment (designs x rho x logbf x b0); drop 000_global_local"
```

---

## Task 5: Full-suite check + build smoke

**Files:** none (verification only).

- [ ] **Step 1: Run the full test suite**

Run: `uv run pytest tests -q`
Expected: all pass (existing + new).

- [ ] **Step 2: Dry-run the pipeline target enumeration**

Run: `uv run snakemake -s logistic_susie_experiments.snk -n results/supercollections/000_markov/MANIFEST.json 2>&1 | tail -20`
Expected: Snakemake plans jobs for `000_markov` (simulate→fit→reduce) without
config/parse errors. (Do not run the full build here — that is a cluster job.)

- [ ] **Step 3: Commit (if any incidental fixes were needed)**

```bash
git add -A
git commit -m "chore(000_markov): suite green + pipeline dry-run clean"
```

---

## Self-Review Notes

- **Spec coverage:** sizing module (T1) ✓; designs/enrichments frozen (T3) ✓; experiment + 6 methods + plots (T4) ✓; remove 000_global_local (T4) ✓; viz dims rho/logbf/null_score (T2) ✓; tests for sizing round-trip + config (T1,T3,T4) ✓.
- **Frozen-table guard:** `test_frozen_b_values_match_sizing` (T3) ties the baked yaml values to `logbf_sizing` so they cannot silently drift.
- **Open detail (non-blocking):** `prior_variance=1.0` is fixed per spec; if a different σ0² is wanted it is a one-line change in `000_markov.yaml` methods. The plot specs assume `facet_row`/`facet_col`/`filter` are supported by the loader's `resolve_plot_specs` (they are, per `experiments/loader.py`); if a plot key is unsupported the implementer should surface it rather than guess.
